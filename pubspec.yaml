name: chapters_db
description: >-
  A lightweight Dart database solution for managing structured chapter content, 
  supporting custom type adapters, multiple box instances, and efficient 
  asynchronous data persistence.
version: 0.4.0
repository: https://github.com/ThanCoder/chapters_db

environment:
  sdk: ^3.11.0

# Add regular dependencies here.
dependencies:
  # path: ^1.9.0

dev_dependencies:
  lints: ^6.0.0
  test: ^1.25.6
